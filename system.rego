################################################################################
# data.system.main allows us to query the REST "/" endpoint and get
# a raw answer without a wrapper
################################################################################
package system

import data.rules

main = rules.main
